<view class="contain">
  <view class="tab">
    <view wx:for="{{tabTitle}}" wx:key="{{index}}" class="tab-son {{num==index?'active':''}}" bindtap='toggle' data-index="{{index}}" data-id="{{item.id}}">{{item.name}}</view>
  </view>
</view>
<!-- <view class="weui-navbar navbar-fix">
  <block wx:for="{{tabTitle}}" wx:key="*this">
    <view id="{{index}}" class="weui-navbar__item {{activeIndex == index ? 'weui-bar__item_on' : ''}}" bindtap="tabClick">
      <view class="weui-navbar__title">{{item.name}}</view>
    </view>
  </block>
  <view class="weui-navbar__slider" style="width: {{sliderWidth}}px; transform: translateX({{sliderOffset}}px); -webkit-transform: translateX({{sliderOffset}}px);"></view>
</view> -->